@model ErrorViewModel
@{
    ViewData["Title"] = "Błędna odpowiedź.";
}

<h1 class="text-danger">Błędna odpowieź.</h1>
<h2 class="text-danger">Spróbuj wpisać inną odpowiedź.</h2>

@* @if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
} *@